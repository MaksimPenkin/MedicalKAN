class_name: IXIRingingDataset
config:
  root: "/media/datasets/IXI_0_1/255"
  transforms:
  - ToTensor

  sampler:
    class_name: CSVSampler
    config:
      filename: "../../data/data_lists/IXI_0_1/test_gibbsnoiseLR.csv"

  load_func: utils.io_utils.read_img
  load_params:
  - key: "sketch"
    shape: [145, 145]
    dtype: float32

  return_filenames: 1
